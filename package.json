{
  "name": "rentapBun",
  "version": "1.08",
  "description": "Create and Fill-in Application Forms offline on your own computer. Faster than npm, but doesn't work in Termux on Android.",
  "module": "back.tsx",
  "type": "module",
  "scripts": {
    "start": "bun run back.tsx& xdg-open http://localhost:3000/",
    "stop": "pkill bun"
  },
  "devDependencies": {
    "@types/react-dom": "^18.2.8",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "react-dom": "^18.2.0"
  }
}
